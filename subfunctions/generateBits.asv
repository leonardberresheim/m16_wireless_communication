%GENERATEBITS generiert eine zufällige Reihe Bits aus
%   Eingabe: 
%       nBits       Anzahl der Bits
%   Ausgabe: 
%       y           (nBits x 1) Vektor der zufällig generierten Bits

function y = generateBits(n(bits)



end;
